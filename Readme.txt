
First copy github url and clone into the pc

Then simply run npm i command

open the cmd and insert the command mongosh

Then open monogodb compass and connect to localhost

Then you can simplu have myuser inside you have user file


Then open vscode and run command npm start to start the server
 
Now you can run the nodejs application and check the nodejs application